rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
    //DBの操作する際の設定 
      allow read, write; 
    //DBロック
      //allow read, write: if false; 
    }
  }
}